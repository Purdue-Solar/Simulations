# Simulations

The software to run interactive simulations directly from non-interactive code.
Use `sim_manager.py add <GitHub url>` to download GitHub repositories with simulations.
Use the generated `manager.py` to run your simulations interactively
Do not push changes to these simulations to their remote repositories
Requires the uv python package manager and python3 in path
